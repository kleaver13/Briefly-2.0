const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav-links');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.15});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const counters=document.querySelectorAll('[data-count]');const countObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target;const target=Number(el.dataset.count);let start=0;const step=Math.max(1,Math.floor(target/70));const tick=()=>{start=Math.min(target,start+step);el.textContent=start.toLocaleString();if(start<target)requestAnimationFrame(tick)};tick();countObserver.unobserve(el)}),{threshold:.6});counters.forEach(c=>countObserver.observe(c));
const quotes=[{text:'Briefly has completely changed how I prepare for meetings. I save hours every week and show up more confident and more prepared.',name:'Sarah Chen',role:'CFO, Acme Corp'},{text:'The context Briefly surfaces before customer calls is exactly what our team used to spend hours assembling manually.',name:'Maya Patel',role:'VP Revenue, Northstar'},{text:'I finally walk into cross-functional meetings knowing the history, owners, and next decisions before the call even starts.',name:'Alex Morgan',role:'COO, Juniper Labs'}];let qi=0;function renderQuote(){const q=quotes[qi];document.querySelector('#quote-text').textContent=q.text;document.querySelector('#quote-name').textContent=q.name;document.querySelector('#quote-role').textContent=q.role}document.querySelector('#prev-quote').addEventListener('click',()=>{qi=(qi-1+quotes.length)%quotes.length;renderQuote()});document.querySelector('#next-quote').addEventListener('click',()=>{qi=(qi+1)%quotes.length;renderQuote()});
const modal=document.querySelector('#site-modal');const form=modal.querySelector('form');const title=document.querySelector('#modal-title');const copy=document.querySelector('#modal-copy');const eyebrow=document.querySelector('#modal-eyebrow');document.querySelectorAll('[data-open-modal]').forEach(btn=>btn.addEventListener('click',()=>{form.classList.remove('success');form.reset();form.querySelector('.form-status').textContent='';const type=btn.dataset.openModal;if(type==='demo'){eyebrow.textContent='BOOK A DEMO';title.textContent='See Briefly in action';copy.textContent='Tell us a little about your team and we’ll schedule a walkthrough.'}else if(type==='login'){eyebrow.textContent='WELCOME BACK';title.textContent='Log in to Briefly';copy.textContent='The customer portal is coming soon. Join the early access list for now.'}else{eyebrow.textContent='GET STARTED';title.textContent='Start using Briefly';copy.textContent='Join the early access list and we’ll be in touch.'}modal.showModal()}));form.addEventListener('submit',e=>{e.preventDefault();form.classList.add('success');form.querySelector('.form-status').textContent='Thanks — your request has been received.'});
